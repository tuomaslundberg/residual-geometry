[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "register-geometry"
version = "0.1.0"
description = "Cross-lingual register geometry in multilingual sentence encoders"
requires-python = ">=3.11"
dependencies = [
    "sentence-transformers>=3.0",
    "scikit-learn>=1.4",
    "scipy>=1.12",
    "numpy>=1.26",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
vis = [
    "umap-learn>=0.5",
    "matplotlib>=3.8",
]
leace = [
    "concept-erasure>=0.4",  # Belrose et al. 2023; installs separately on LUMI
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
